// Enhanced Mobile Phase Detail Component
const MobilePhaseDetail: React.FC<MobilePhaseDetailProps> = ({
  mobilePhase,
  onStepChange,
}) => {
  const [isExpanded, setIsExpanded] = useState(true);

  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      exit={{ opacity: 0, y: -20 }}
      className="relative overflow-hidden rounded-2xl border-2 border-blue-400 bg-gradient-to-br from-blue-50 via-white to-blue-100 shadow-xl mb-6 hover:shadow-2xl transition-all duration-300"
    >
      {/* Decorative gradient overlay */}
      
      {/* Header */}
      <motion.div
        className="relative cursor-pointer select-none bg-gradient-to-r from-blue-500 via-blue-600 to-blue-500 hover:from-blue-600 hover:via-blue-700 hover:to-blue-600 transition-all duration-300"
        onClick={() => setIsExpanded(!isExpanded)}
        whileHover={{ scale: 1.01 }}
        whileTap={{ scale: 0.99 }}
      >
        <div className="flex items-center justify-between px-6 py-4">
          <div className="flex items-center gap-4">
            <motion.div
              animate={{ rotate: isExpanded ? 0 : 360 }}
              transition={{ duration: 0.5 }}
              className="p-2 bg-white/20 rounded-full backdrop-blur-sm"
            >
              <Beaker className="w-6 h-6 text-white" />
            </motion.div>
            <div>
              <h4 className="text-lg font-bold text-white tracking-wide">
                {mobilePhase.label}
              </h4>
              <p className="text-xs text-blue-100 font-medium">Preparation Details</p>
            </div>
          </div>
          
          <motion.div
            animate={{ rotate: isExpanded ? 180 : 0 }}
            transition={{ duration: 0.3 }}
            className="p-2 bg-white/20 rounded-full backdrop-blur-sm"
          >
            <ChevronDown className="w-5 h-5 text-white" />
          </motion.div>
        </div>

        {/* Progress indicator bar */}
        <div className="absolute bottom-0 left-0 right-0 h-1 bg-blue-900/20">
          <motion.div
            className="h-full bg-gradient-to-r from-cyan-400 to-blue-300"
            initial={{ width: "0%" }}
            animate={{ 
              width: `${(mobilePhase.steps.filter(s => s.value).length / mobilePhase.steps.length) * 100}%` 
            }}
            transition={{ duration: 0.5 }}
          />
        </div>
      </motion.div>

      {/* Content */}
      <AnimatePresence>
        {isExpanded && (
          <motion.div
            initial={{ height: 0, opacity: 0 }}
            animate={{ height: "auto", opacity: 1 }}
            exit={{ height: 0, opacity: 0 }}
            transition={{ duration: 0.3 }}
            className="overflow-hidden"
          >
            <div className="p-6 space-y-4">
              {mobilePhase.steps.map((step, index) => {
                const isWeighing = step.name === "Weighing";
                const isPH = step.name === "PH";
                const isSonication = step.name === "Sonication";
                const isFiltration = step.name === "Filtration";

                return (
                  <motion.div
                    key={step.name}
                    initial={{ opacity: 0, x: -20 }}
                    animate={{ opacity: 1, x: 0 }}
                    transition={{ delay: index * 0.1 }}
                    className="group relative bg-white rounded-xl p-5 border-2 border-blue-200 hover:border-blue-400 hover:shadow-lg transition-all duration-300"
                  >
                    {/* Step number badge */}
                    <div className="absolute -top-3 -left-3 w-8 h-8 bg-gradient-to-br from-blue-500 to-blue-700 rounded-full flex items-center justify-center text-white font-bold text-sm shadow-lg group-hover:scale-110 transition-transform">
                      {index + 1}
                    </div>

                    <div className="grid grid-cols-[160px_1fr] gap-4 items-start">
                      <div className="flex items-center gap-2 pt-2">
                        <div className="w-1 h-8 bg-gradient-to-b from-blue-400 to-blue-600 rounded-full"></div>
                        <span className="font-bold text-blue-900 text-sm">{step.name}</span>
                      </div>
                      
                      <div className="flex flex-col gap-3">
                        {isWeighing && (
                          <>
                            <div className="flex flex-wrap items-center gap-2 text-sm">
                              <span className="text-gray-700 font-medium">Weigh accurately</span>
                              <input
                                type="text"
                                value={step.value}
                                onChange={(e) => onStepChange(mobilePhase.id, step.name, "value", e.target.value)}
                                placeholder="Amount"
                                className="w-28 px-3 py-2 border-2 border-blue-300 rounded-lg text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all hover:border-blue-400"
                              />
                              <div className="relative">
                                <select
                                  value={step.unit}
                                  onChange={(e) => onStepChange(mobilePhase.id, step.name, "unit", e.target.value)}
                                  className="appearance-none w-20 px-3 py-2 pr-8 border-2 border-blue-300 rounded-lg text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 bg-white transition-all hover:border-blue-400 cursor-pointer"
                                >
                                  {["g", "mg", "kg"].map((unit) => (
                                    <option key={unit} value={unit}>{unit}</option>
                                  ))}
                                </select>
                                <ChevronDown className="absolute right-2 top-1/2 transform -translate-y-1/2 w-4 h-4 text-blue-600 pointer-events-none" />
                              </div>
                              <span className="text-gray-700 font-medium">of</span>
                              <input
                                type="text"
                                value={step.solventChemical || ""}
                                onChange={(e) => onStepChange(mobilePhase.id, step.name, "solventChemical", e.target.value)}
                                placeholder="Solvent/Chemical"
                                className="flex-grow min-w-[180px] px-3 py-2 border-2 border-blue-300 rounded-lg text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all hover:border-blue-400"
                              />
                            </div>
                            <div className="flex items-center gap-2 text-sm bg-blue-50 rounded-lg p-2">
                              <span className="text-gray-700 font-medium">Log Book ID:</span>
                              <input
                                type="text"
                                value={step.logBookID}
                                onChange={(e) => onStepChange(mobilePhase.id, step.name, "logBookID", e.target.value)}
                                placeholder="Enter Log ID"
                                className="flex-grow px-3 py-2 border-2 border-blue-300 rounded-lg text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 bg-white transition-all hover:border-blue-400"
                              />
                            </div>
                          </>
                        )}

                        {isPH && (
                          <>
                            <div className="flex flex-wrap items-center gap-2 text-sm">
                              <span className="text-gray-700 font-medium">Adjust the pH to</span>
                              <input
                                type="text"
                                value={step.value}
                                onChange={(e) => onStepChange(mobilePhase.id, step.name, "value", e.target.value)}
                                placeholder="pH value"
                                className="w-28 px-3 py-2 border-2 border-blue-300 rounded-lg text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all hover:border-blue-400"
                              />
                            </div>
                            <div className="flex items-center gap-2 text-sm bg-blue-50 rounded-lg p-2">
                              <span className="text-gray-700 font-medium">Log Book ID:</span>
                              <input
                                type="text"
                                value={step.logBookID}
                                onChange={(e) => onStepChange(mobilePhase.id, step.name, "logBookID", e.target.value)}
                                placeholder="Enter Log ID"
                                className="flex-grow px-3 py-2 border-2 border-blue-300 rounded-lg text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 bg-white transition-all hover:border-blue-400"
                              />
                            </div>
                          </>
                        )}

                        {isFiltration && (
                          <div className="flex flex-wrap items-center gap-2 text-sm">
                            <span className="text-gray-700 font-medium">Filter the mobile phase from</span>
                            <input
                              type="text"
                              value={step.value}
                              onChange={(e) => onStepChange(mobilePhase.id, step.name, "value", e.target.value)}
                              placeholder="Size"
                              className="w-28 px-3 py-2 border-2 border-blue-300 rounded-lg text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all hover:border-blue-400"
                            />
                            <div className="relative">
                              <select
                                value={step.unit}
                                onChange={(e) => onStepChange(mobilePhase.id, step.name, "unit", e.target.value)}
                                className="appearance-none w-24 px-3 py-2 pr-8 border-2 border-blue-300 rounded-lg text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 bg-white transition-all hover:border-blue-400 cursor-pointer"
                              >
                                {["micron", "µm", "mm"].map((unit) => (
                                  <option key={unit} value={unit}>{unit}</option>
                                ))}
                              </select>
                              <ChevronDown className="absolute right-2 top-1/2 transform -translate-y-1/2 w-4 h-4 text-blue-600 pointer-events-none" />
                            </div>
                            <span className="text-gray-700 font-medium">filter</span>
                          </div>
                        )}

                        {isSonication && (
                          <>
                            <div className="flex flex-wrap items-center gap-2 text-sm">
                              <span className="text-gray-700 font-medium">Sonicate the mobile phase for</span>
                              <input
                                type="text"
                                value={step.value}
                                onChange={(e) => onStepChange(mobilePhase.id, step.name, "value", e.target.value)}
                                placeholder="Time"
                                className="w-24 px-3 py-2 border-2 border-blue-300 rounded-lg text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 transition-all hover:border-blue-400"
                              />
                              <span className="text-gray-700 font-medium">{step.unit}</span>
                            </div>
                            <div className="flex items-center gap-2 text-sm bg-blue-50 rounded-lg p-2">
                              <span className="text-gray-700 font-medium">Mobile Phase ID:</span>
                              <input
                                type="text"
                                value={step.mobilePhaseID}
                                onChange={(e) => onStepChange(mobilePhase.id, step.name, "mobilePhaseID", e.target.value)}
                                placeholder="Enter ID"
                                className="flex-grow px-3 py-2 border-2 border-blue-300 rounded-lg text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-blue-500 focus:border-blue-500 bg-white transition-all hover:border-blue-400"
                              />
                            </div>
                          </>
                        )}
                      </div>
                    </div>
                  </motion.div>
                );
              })}
            </div>
          </motion.div>
        )}
      </AnimatePresence>
    </motion.div>
  );
};

// Enhanced Disso Media Detail Component
const DissoMediaDetail: React.FC<DissoMediaDetailProps> = ({
  dissoMedia,
  onStepChange,
}) => {
  const [isExpanded, setIsExpanded] = useState(true);

  return (
    <motion.div
      initial={{ opacity: 0, y: 20 }}
      animate={{ opacity: 1, y: 0 }}
      exit={{ opacity: 0, y: -20 }}
      className="relative overflow-hidden rounded-2xl border-2 border-amber-400 bg-gradient-to-br from-amber-50 via-white to-yellow-100 shadow-xl mb-6 hover:shadow-2xl transition-all duration-300"
    >
      {/* Decorative gradient overlay */}
      <div className="absolute top-0 left-0 right-0 h-1 bg-gradient-to-r from-amber-400 via-yellow-500 to-amber-400"></div>
      
      {/* Header */}
      <motion.div
        className="relative cursor-pointer select-none bg-gradient-to-r from-amber-500 via-yellow-600 to-amber-500 hover:from-amber-600 hover:via-yellow-700 hover:to-amber-600 transition-all duration-300"
        onClick={() => setIsExpanded(!isExpanded)}
        whileHover={{ scale: 1.01 }}
        whileTap={{ scale: 0.99 }}
      >
        <div className="flex items-center justify-between px-6 py-4">
          <div className="flex items-center gap-4">
            <motion.div
              animate={{ rotate: isExpanded ? 0 : 360 }}
              transition={{ duration: 0.5 }}
              className="p-2 bg-white/20 rounded-full backdrop-blur-sm"
            >
              <Beaker className="w-6 h-6 text-white" />
            </motion.div>
            <div>
              <h4 className="text-lg font-bold text-white tracking-wide">
                {dissoMedia.label}
              </h4>
              <p className="text-xs text-yellow-100 font-medium">Preparation Details</p>
            </div>
          </div>
          
          <motion.div
            animate={{ rotate: isExpanded ? 180 : 0 }}
            transition={{ duration: 0.3 }}
            className="p-2 bg-white/20 rounded-full backdrop-blur-sm"
          >
            <ChevronDown className="w-5 h-5 text-white" />
          </motion.div>
        </div>

        {/* Progress indicator bar */}
        <div className="absolute bottom-0 left-0 right-0 h-1 bg-amber-900/20">
          <motion.div
            className="h-full bg-gradient-to-r from-yellow-300 to-amber-300"
            initial={{ width: "0%" }}
            animate={{ 
              width: `${(dissoMedia.steps.filter(s => s.value).length / dissoMedia.steps.length) * 100}%` 
            }}
            transition={{ duration: 0.5 }}
          />
        </div>
      </motion.div>

      {/* Content */}
      <AnimatePresence>
        {isExpanded && (
          <motion.div
            initial={{ height: 0, opacity: 0 }}
            animate={{ height: "auto", opacity: 1 }}
            exit={{ height: 0, opacity: 0 }}
            transition={{ duration: 0.3 }}
            className="overflow-hidden"
          >
            <div className="p-6 space-y-4">
              {dissoMedia.steps.map((step, index) => {
                const isWeighing = step.name === "Weighing";
                const isPH = step.name === "PH";
                const isSonication = step.name === "Sonication";
                const isFiltration = step.name === "Filtration";

                return (
                  <motion.div
                    key={step.name}
                    initial={{ opacity: 0, x: -20 }}
                    animate={{ opacity: 1, x: 0 }}
                    transition={{ delay: index * 0.1 }}
                    className="group relative bg-white rounded-xl p-5 border-2 border-amber-200 hover:border-amber-400 hover:shadow-lg transition-all duration-300"
                  >
                    {/* Step number badge */}
                    <div className="absolute -top-3 -left-3 w-8 h-8 bg-gradient-to-br from-amber-500 to-yellow-600 rounded-full flex items-center justify-center text-white font-bold text-sm shadow-lg group-hover:scale-110 transition-transform">
                      {index + 1}
                    </div>

                    <div className="grid grid-cols-[160px_1fr] gap-4 items-start">
                      <div className="flex items-center gap-2 pt-2">
                        <div className="w-1 h-8 bg-gradient-to-b from-amber-400 to-yellow-600 rounded-full"></div>
                        <span className="font-bold text-amber-900 text-sm">{step.name}</span>
                      </div>
                      
                      <div className="flex flex-col gap-3">
                        {isWeighing && (
                          <>
                            <div className="flex flex-wrap items-center gap-2 text-sm">
                              <span className="text-gray-700 font-medium">Weigh accurately</span>
                              <input
                                type="text"
                                value={step.value}
                                onChange={(e) => onStepChange(dissoMedia.id, step.name, "value", e.target.value)}
                                placeholder="Amount"
                                className="w-28 px-3 py-2 border-2 border-amber-300 rounded-lg text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-amber-500 focus:border-amber-500 transition-all hover:border-amber-400"
                              />
                              <div className="relative">
                                <select
                                  value={step.unit}
                                  onChange={(e) => onStepChange(dissoMedia.id, step.name, "unit", e.target.value)}
                                  className="appearance-none w-20 px-3 py-2 pr-8 border-2 border-amber-300 rounded-lg text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-amber-500 focus:border-amber-500 bg-white transition-all hover:border-amber-400 cursor-pointer"
                                >
                                  {["g", "mg", "kg"].map((unit) => (
                                    <option key={unit} value={unit}>{unit}</option>
                                  ))}
                                </select>
                                <ChevronDown className="absolute right-2 top-1/2 transform -translate-y-1/2 w-4 h-4 text-amber-600 pointer-events-none" />
                              </div>
                              <span className="text-gray-700 font-medium">of</span>
                              <input
                                type="text"
                                value={step.solventChemical || ""}
                                onChange={(e) => onStepChange(dissoMedia.id, step.name, "solventChemical", e.target.value)}
                                placeholder="Solvent/Chemical"
                                className="flex-grow min-w-[180px] px-3 py-2 border-2 border-amber-300 rounded-lg text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-amber-500 focus:border-amber-500 transition-all hover:border-amber-400"
                              />
                            </div>
                            <div className="flex items-center gap-2 text-sm bg-amber-50 rounded-lg p-2">
                              <span className="text-gray-700 font-medium">Log Book ID:</span>
                              <input
                                type="text"
                                value={step.logBookID}
                                onChange={(e) => onStepChange(dissoMedia.id, step.name, "logBookID", e.target.value)}
                                placeholder="Enter Log ID"
                                className="flex-grow px-3 py-2 border-2 border-amber-300 rounded-lg text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-amber-500 focus:border-amber-500 bg-white transition-all hover:border-amber-400"
                              />
                            </div>
                          </>
                        )}

                        {isPH && (
                          <>
                            <div className="flex flex-wrap items-center gap-2 text-sm">
                              <span className="text-gray-700 font-medium">Adjust the pH to</span>
                              <input
                                type="text"
                                value={step.value}
                                onChange={(e) => onStepChange(dissoMedia.id, step.name, "value", e.target.value)}
                                placeholder="pH value"
                                className="w-28 px-3 py-2 border-2 border-amber-300 rounded-lg text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-amber-500 focus:border-amber-500 transition-all hover:border-amber-400"
                              />
                            </div>
                            <div className="flex items-center gap-2 text-sm bg-amber-50 rounded-lg p-2">
                              <span className="text-gray-700 font-medium">Log Book ID:</span>
                              <input
                                type="text"
                                value={step.logBookID}
                                onChange={(e) => onStepChange(dissoMedia.id, step.name, "logBookID", e.target.value)}
                                placeholder="Enter Log ID"
                                className="flex-grow px-3 py-2 border-2 border-amber-300 rounded-lg text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-amber-500 focus:border-amber-500 bg-white transition-all hover:border-amber-400"
                              />
                            </div>
                          </>
                        )}

                        {isFiltration && (
                          <div className="flex flex-wrap items-center gap-2 text-sm">
                            <span className="text-gray-700 font-medium">Filter the disso media from</span>
                            <input
                              type="text"
                              value={step.value}
                              onChange={(e) => onStepChange(dissoMedia.id, step.name, "value", e.target.value)}
                              placeholder="Size"
                              className="w-28 px-3 py-2 border-2 border-amber-300 rounded-lg text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-amber-500 focus:border-amber-500 transition-all hover:border-amber-400"
                            />
                            <div className="relative">
                              <select
                                value={step.unit}
                                onChange={(e) => onStepChange(dissoMedia.id, step.name, "unit", e.target.value)}
                                className="appearance-none w-24 px-3 py-2 pr-8 border-2 border-amber-300 rounded-lg text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-amber-500 focus:border-amber-500 bg-white transition-all hover:border-amber-400 cursor-pointer"
                              >
                                {["micron", "µm", "mm"].map((unit) => (
                                  <option key={unit} value={unit}>{unit}</option>
                                ))}
                              </select>
                              <ChevronDown className="absolute right-2 top-1/2 transform -translate-y-1/2 w-4 h-4 text-amber-600 pointer-events-none" />
                            </div>
                            <span className="text-gray-700 font-medium">filter</span>
                          </div>
                        )}

                        {isSonication && (
                          <div className="flex flex-wrap items-center gap-2 text-sm">
                            <span className="text-gray-700 font-medium">Sonicate the disso media for</span>
                            <input
                              type="text"
                              value={step.value}
                              onChange={(e) => onStepChange(dissoMedia.id, step.name, "value", e.target.value)}
                              placeholder="Time"
                              className="w-24 px-3 py-2 border-2 border-amber-300 rounded-lg text-sm font-semibold focus:outline-none focus:ring-2 focus:ring-amber-500 focus:border-amber-500 transition-all hover:border-amber-400"
                            />
                            <span className="text-gray-700 font-medium">{step.unit}</span>
                          </div>
                        )}
                      </div>
                    </div>
                  </motion.div>
                );
              })}
            </div>
          </motion.div>
        )}
      </AnimatePresence>
    </motion.div>
  );
};